#!/bin/sh
CC=gcc
SRC=lucas_num.c

$CC -Wall -g $SRC && (
	cat input | ./a.exe \
	| tr -d "\r" \  # strip carriage returns - just in case
	> actual &&
	diff -u actual exp && echo "pass!" || (
		echo "failed!";
		cat actual;
	)
)
