#include <stdio.h>

int double_digit(int num);

int main () {
  int num;

  /* loop on input */
  while (scanf("%d", &num) == 1)
    printf("%d\n", double_digit(num));

  return 0;
}

int double_digit(int num) {
	/* TODO implement */

	return 0;
}
